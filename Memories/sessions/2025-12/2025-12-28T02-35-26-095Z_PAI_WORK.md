---type: WORK
timestamp: 2025-12-28T02:35:26.095Z
sessionId: ses_49d3a95d4ffeYUQgME8ZCfsFJf
---
# WORK Capture

## Summary
Proceeding with Option 1 (Refactor) and presenting the first section of the design: Directory Structure.

## Completed
Presented Directory Structure Design.

I'm using the **brainstorming** skill to present the design.

Proceeding with **Option 1: Refactor Existing Repository**.

### Design Section 1: Directory Structure & Standardization

To treat `pai-opencode` as an Adapter, we need a strict separation between **PAI Core Content** (which mirrors the official repo) and **OpenCode Specifics**.

**Proposed Structure:**

```text
~/Workspaces/pai-opencode/
├── .git/
├── .gitignore             # Standard PAI gitignore
├── Skills/                # [PAI Core] Official Skills (CORE, etc.)
├── Tools/                 # [PAI Core] Official Tools
├── Memories/              # [PAI Core] User memory storage
├── docs/                  # [PAI Core] Official docs
├── LICENSE                # [PAI Core]
├── README.md              # [PAI Core] Official README
│
├── opencode/              # [Adapter] ALL OpenCode-specific logic lives here
│   ├── plugin/            # The TypeScript plugin code (pai-adapter.ts)
│   ├── config/            # Default configurations
│   └── scripts/           # Setup/Verify scripts (setup-adapter.sh)
│
└── settings.json          # [Adapter] Local configuration (ignored by git)
```

**Cleanup Strategy:**
1.  **Move:** Shift `pai-core.ts` and related custom logic into `opencode/plugin/`.
2.  **Delete/Archive:** Remove root-level scripts (`test-pai-bash.sh`, `check-pai-type.sh`) or move them to `opencode/scripts/`.
3.  **Preserve:** Keep `.git` history but start a "fresh" commit for the restructure.

**Does this directory structure look right to you?**
A) Yes, this structure works.
B) No, `opencode/` folder should be hidden (e.g., `.opencode/`).
C) I want to keep the scripts in the root.

## Raw
```json
{
  "summary": "Proceeding with Option 1 (Refactor) and presenting the first section of the design: Directory Structure.",
  "completed": "Presented Directory Structure Design.\n\nI'm using the **brainstorming** skill to present the design.\n\nProceeding with **Option 1: Refactor Existing Repository**.\n\n### Design Section 1: Directory Structure & Standardization\n\nTo treat `pai-opencode` as an Adapter, we need a strict separation between **PAI Core Content** (which mirrors the official repo) and **OpenCode Specifics**.\n\n**Proposed Structure:**\n\n```text\n~/Workspaces/pai-opencode/\n├── .git/\n├── .gitignore             # Standard PAI gitignore\n├── Skills/                # [PAI Core] Official Skills (CORE, etc.)\n├── Tools/                 # [PAI Core] Official Tools\n├── Memories/              # [PAI Core] User memory storage\n├── docs/                  # [PAI Core] Official docs\n├── LICENSE                # [PAI Core]\n├── README.md              # [PAI Core] Official README\n│\n├── opencode/              # [Adapter] ALL OpenCode-specific logic lives here\n│   ├── plugin/            # The TypeScript plugin code (pai-adapter.ts)\n│   ├── config/            # Default configurations\n│   └── scripts/           # Setup/Verify scripts (setup-adapter.sh)\n│\n└── settings.json          # [Adapter] Local configuration (ignored by git)\n```\n\n**Cleanup Strategy:**\n1.  **Move:** Shift `pai-core.ts` and related custom logic into `opencode/plugin/`.\n2.  **Delete/Archive:** Remove root-level scripts (`test-pai-bash.sh`, `check-pai-type.sh`) or move them to `opencode/scripts/`.\n3.  **Preserve:** Keep `.git` history but start a \"fresh\" commit for the restructure.\n\n**Does this directory structure look right to you?**\nA) Yes, this structure works.\nB) No, `opencode/` folder should be hidden (e.g., `.opencode/`).\nC) I want to keep the scripts in the root."
}
```